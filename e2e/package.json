{
  "private": true,
  "scripts": {
    "serve": "cd .. && make serve",
    "run": "cypress run",
    "open": "cypress open",
    "e2e:run": "start-server-and-test serve http://localhost:8000 run",
    "e2e:open": "start-server-and-test serve http://localhost:8000 open"
  },
  "dependencies": {
    "cypress": "^8.3.0",
    "start-server-and-test": "^1.13.1"
  }
}
